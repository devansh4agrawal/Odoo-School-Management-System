<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="student_relation_data" model="ir.ui.view">
        <field name="name">relation.profile</field>
        <field name="model">relation.profile</field>
        <field name="arch" type="xml">
            <tree create="false" delete="false">
                <field name="rel" />
            </tree>
        </field>
    </record>

    <record
        id="action_relation_profile" model="ir.actions.act_window">
        <field name="name">Relation</field>
        <field name="res_model">relation.profile</field>
        <field name="view_mode">tree</field>
    </record>
</odoo>